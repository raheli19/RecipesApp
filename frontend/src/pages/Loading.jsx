// Do a Loading page with a spinner
import React from "react";
import CircularProgress from "@mui/material/CircularProgress";

export default function Loading() {
    return (
        <div>
        <CircularProgress />
        </div>
    );
    }
